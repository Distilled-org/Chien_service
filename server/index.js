// const express = require('express');
// const db = require('./db');
// const app = express();
// const PORT = 8080;

// // req.url and the req.method
// app.use((req, res, next) => {
//   console.log(`${req.method} request for ${req.url}`);
//   next();
// });
// app.use(express.static('public'));
// app.use(express.json());

// //write a get rewuest for it
// app.get("/", (req, res)=> {

// });